# DevFlow 开发需求文档 — v2.15.0

> 文档类型：开发需求文档（主文件）
> 版本：v2.15.0
> 日期：2026-08-01
> 作者：RA-DevFlow-Dev

---

## 1. 入场检查

| 检查项 | 结果 | 证据 |
|:-------|:----:|:-----|
| Step 0 版本规划已批准 | ✅ | 版本规划评审记录 v1.4，结论：批准进入 Step 1 |
| 版本目标已明确 | ✅ | 单版本规划文档 v1.3，3 项目标均含可量化成功指标 |
| 包含/不包含范围已明确 | ✅ | 5 项包含需求（V215-001~005）+ 6 项不包含需求 |
| 本版本 Backlog 已形成 | ✅ | Backlog v1.5，5 条 BL 条目（BL-215-001~005） |
| Phase 计划已确认 | ✅ | Phase 迭代计划 v1.5，3 个 Phase 递进 |
| P0/P1 优先级已确认 | ✅ | 2 项 P0 + 2 项 P1 + 1 项 P2 |
| Stage 0 审计闭环 | ✅ | 阶段审计报告 v1.6，6 项发现项全部闭环 |
| 关键依赖已验证 | ✅ | 10 项依赖均确认就绪/可用 |
| 无未确认的范围争议 | ✅ | version.json 弃用决策已执行，无遗留争议 |

---

## 2. F1: T1-T4 四层测试架构集成到 testing-stage-execution（BL-215-001）

| 维度 | 内容 |
|:-----|:------|
| **描述** | 将 T1-T4 四层测试架构方案（v1.4.0）完整集成到 testing-stage-execution 主控技能。T1-T4 架构定义四个测试层级：T1 契约层（路由映射表 diff + OpenAPI 契约测试）、T2 接口层（三要素校验 + 断言分级）、T3 页面/集成层（T3a 全页面巡检 + T3b 深度用例）、T4 验收层（业务流走查 + 人机协同）。架构源自 OpenLLM 测试实践教训（BUG-v2.9.0-MODEL-001：模型管理按钮空实现未被测试覆盖），旨在解决"接口通过≠页面可用"的系统性测试盲区。 |
| **处理** | 在 testing-stage-execution/SKILL.md 中新增和增强以下 13 项核心改动：|
| ① T1-T4 层级总览章节 | 新增"T1-T4 四层测试架构"章节，包含层级总览表（4 层：名称/目标/核心内容/通过标准/关键产出）、层间追溯要求（T1→T2→T3→T4 覆盖率 100%）、项目类型适配（全栈项目/纯后端项目/单服务纯 API 项目，含不适用声明规则） |
| ② 增强强制测试矩阵 | 强制测试矩阵 API 测试行增强：新增三要素校验（状态码 + 响应结构 + 边界参数）和"接口通过≠页面可用"显式声明 |
| ③ T3 两档分层规范 | 新增 T3a 全页面巡检规范（含六步闭环工作流：清单盘点→自动化巡检→问题分类→根因定位→修复回归→报告更新）和 T3b 深度用例规范（CRUD/筛选/分页/错误态） |
| ④ T3a 巡检信号和问题分类 | 6 类巡检信号（HTTP≥400/console.error/pageerror/接口200但渲染空/静默失败/表单提交响应）+ 7 类标准问题分类（A-G 类，含 F 类静默失败检测 + G 类表单提交错误被吞没）+ 根因定位手段 + 关键按钮清单 7 类（添加/创建/新建/删除/编辑/导出/刷新）+ 对话框清理逻辑 |
| ⑤ T4 业务流走查模板 | 新增 T4 业务流走查清单模板 + 人机协同定位 + 人工测试检查清单（5 大类：基础功能/CRUD链路/边界异常/视觉交互/真实用户流）+ 抽样策略（核心模块100%/辅助模块30%） |
| ⑥ 四轨映射 | 四轨并行工作流映射中新增 T 层级标注（整体→T1/T4、后端→T2、前端→T3） |
| ⑦ 通过标准增强 | 通过标准新增：层间追溯校验（T1→T2→T3→T4 覆盖率 100%）+ 软断言清零 + 人工测试执行率 100% |
| ⑧ 反模式新增 | 反模式清单新增 4 条 T1-T4 相关条目（只做契约测试不做页面巡检/软断言掩盖功能缺失/人工测试可选/CRUD 仅做只读） |
| ⑨ 断言分级规范 | 断言分级（L1 硬断言/L2 条件断言/L3 存在性断言）+ 禁止模式清单（3 种：if-else 软断言/try-except 吞异常/超宽容断言）+ 推荐断言模式（L1/L2 标准代码模板）+ 断言策略速查表（6 种场景） |
| ⑩ CRUD 全覆盖规则 | CRUD 全覆盖硬性规则（管理类模块 C+R+U+D 至少 3 类）+ 用例设计规范 |
| ⑪ 测试覆盖矩阵 | 测试覆盖矩阵（模块×用例类型）+ 测试度量体系（7 项指标：用例覆盖率/硬断言比例/软断言数量/巡检覆盖率/静默失败检测覆盖率/缺陷逃逸率/人工测试执行率）+ 覆盖缺口分析方法论（缺口分析表+分级规则 P0/P1/P2+追踪闭环） |
| ⑫ 术语表 | 术语表（12 个核心术语：CRUD/软断言/硬断言/条件断言/存在性断言/静默失败/行为信号检测/表单提交错误被吞没/缺陷逃逸率/覆盖矩阵/覆盖缺口/巡检工具） |
| ⑬ 根因映射表+验收标准 | 根因映射表（4 大根因→改进项：测试设计原则缺失/巡检工具能力不足/测试度量缺失/人工测试定位模糊）+ 改进项验收标准（9 项验收标准+验证方法） |
| **验收标准** | ① testing-stage-execution 中可检索到"T1-T4 四层测试架构"章节标题 ② T3a 六步闭环工作流完整（6 步均有标题和说明）③ 7 类标准问题分类表格存在（A-G 类）④ 6 类巡检信号表存在（含表单提交响应检测）⑤ 关键按钮清单包含 7 类（含"新建""刷新"）⑥ 巡检规范包含对话框清理逻辑代码片段 ⑦ 断言分级规范（L1/L2/L3）和禁止模式清单（3 种）存在 ⑧ 推荐断言模式（L1/L2 标准模板）和断言策略速查表（6 种场景）存在 ⑨ CRUD 全覆盖硬性规则存在 ⑩ 人机协同定位+人工测试检查清单（5 大类）+抽样策略存在 ⑪ 测试覆盖矩阵和度量体系（7 项指标）存在 ⑫ 覆盖缺口分析方法论（缺口分析表+分级规则+追踪闭环）存在 ⑬ 术语表（≥12 个核心术语）存在 ⑭ 根因映射表（4 大根因→改进项）存在 ⑮ 改进项验收标准（≥9 项）存在 ⑯ 四轨并行工作流映射包含 T 层级标注 ⑰ 通过标准包含"层间追溯校验"+"软断言清零"+"人工测试执行率 100%" ⑱ 反模式包含至少 4 条 T1-T4 相关条目 |
| **优先级** | 🔴 P0 |
| **关联** | V215-001, 设计方案 v1.4.0 |

---

## 3. F2: 路由映射表 diff 机制增强 api-contract-management（BL-215-002）

| 维度 | 内容 |
|:-----|:------|
| **描述** | 在 api-contract-management 的 Step 4 测试章节新增"路由映射表 diff 机制"，使前后端路由不一致问题（422/404 类）能在测试阶段被自动检测。当前 api-contract-management Step 4 章节有 MSW Mock 联调、契约测试和字段一致性校验，但缺少路由级别的自动 diff 能力。 |
| **处理** | ① 定义 YAML 格式路由→字段映射表（包含路由路径、HTTP 方法、请求字段、响应字段、前端调用文件位置）② 定义 diff 脚本规范（从后端路由定义和前端 API 调用代码提取映射，自动 diff 输出差异清单）③ 检查清单新增"路由映射表 diff 已执行"项 |
| **验收标准** | ① api-contract-management 包含 YAML 格式路由映射表定义 ② diff 脚本规范有明确的输入/输出/执行方式描述 ③ 检查清单包含"路由映射表 diff 已执行"项 ④ 新增内容与现有 Step 4 章节结构一致，不破坏现有 MSW/契约测试流程 |
| **优先级** | 🟡 P1 |
| **关联** | V215-002 |

---

## 4. F3: Stage4 产出物清单新增 T1-T4 相关产出（BL-215-003）

| 维度 | 内容 |
|:-----|:------|
| **描述** | 在 Stage4 产出物清单中新增 5 项 T1-T4 架构相关的强制产出物，确保测试架构落地后有对应的门禁保障。当前清单仅有 6 项基础产出物（测试计划/报告/用例/回溯审计/执行证据/阶段审计），缺少 T1-T4 架构特有的产出。 |
| **处理** | 新增 5 项强制产出：① T1-T4 层间追溯矩阵（T1 契约→T2 接口→T3 页面→T4 验收的覆盖追踪）② 全页面巡检问题表（T3a 巡检证据：页面路径×问题类型×问题详情×修复状态）③ UAT 走查清单（T4 人工测试检查清单执行记录）④ 测试覆盖矩阵（模块×用例类型二维矩阵）⑤ 测试度量报告（7 项指标度量结果） |
| **验收标准** | ① 产出物清单 Stage4 包含 5 项新条目 ② 每项新条目有明确的文件路径模式 ③ 新条目标记为"强制"类型 ④ 清单版本号更新为 v2.15.0 |
| **优先级** | 🟡 P1 |
| **关联** | V215-003 |

---

## 5. F4: 版本号单一事实源落地 TD-031 偿还（BL-215-004）

| 维度 | 内容 |
|:-----|:------|
| **描述** | 偿还 TD-031（P0 架构债务）：确立 devflow-config.json 的 devflowVersion 为版本号唯一事实源，彻底弃用并删除 version.json，建立发布时自动同步链路。此需求已部分执行（version.json 已删除，引用已更新），需在 Step 3 完成剩余的发布流程增强和脚本更新。 |
| **处理** | ① 确立 devflow-config.json 的 devflowVersion 为版本号唯一事实源（已执行：deprecatedFiles 移除 version.json）② 发布流程增强：release.ps1 增加 JSON 配置同步步骤（devflow-config.json → state.json 自动同步）③ validate-version-header.ps1 Phase 1 JSON 一致性检查纳入发布门禁（3 份配置：devflow-config/project-config/state）④ 同步安装副本（.devflow/skills/ + .trae/skills/）⑤ 更新 sync-skills.ps1 和 download-devflow.ps1 脚本引用（已执行：从 version.json 改为 devflow-config.json） |
| **验收标准** | ① devflow-config.json 的 devflowVersion 包含 `_meta.sourceOfTruth=true` 标注 ② 根目录 version.json 和 .devflow/version.json 已删除 ③ release.ps1 包含 JSON 配置同步步骤（devflow-config → state.json）④ validate-version-header.ps1 检查 3 份 JSON 配置一致性 ⑤ sync-skills.ps1 从 devflow-config.json 读取版本号 ⑥ download-devflow.ps1 从 devflow-config.json 读取版本号 ⑦ .devflow/skills/ 和 .trae/skills/ 安装副本已同步 |
| **优先级** | 🔴 P0 |
| **关联** | V215-004, TD-031 |
| **执行状态** | 🔧 进行中（version.json 删除+引用更新+安装副本同步已完成；release.ps1 自动同步链路待 Step 3 实现） |

---

## 6. F5: Git post-push 自动备份 hook 纳入规范（BL-215-005）

| 维度 | 内容 |
|:-----|:------|
| **描述** | 将已实现的 Git pre-push hook（.devflow/hooks/post-push + push-with-backup.ps1）纳入 code-version-backup-management 技能规范，作为三远程架构（origin/backup/github）的标准自动化组件。hook 已安装并验证，仅需文档化纳入规范。 |
| **处理** | ① code-version-backup-management SKILL.md 新增"自动备份 hook"章节（hook 工作原理、安装方式、日志规范）② devflow-init SKILL.md 安装流程纳入 hook 安装步骤 ③ 日志规范（.devflow/logs/backup-hook.log，含时间戳、推送引用、SHA 验证、成功/失败状态） |
| **验收标准** | ① code-version-backup-management 包含"自动备份 hook"章节 ② devflow-init 安装流程包含 hook 安装步骤 ③ 日志规范定义了 .devflow/logs/backup-hook.log 的格式和字段 |
| **优先级** | 🟢 P2 |
| **关联** | V215-005 |

---

## 7. 非功能需求

| 维度 | 要求 | 验证方法 |
|:-----|:-----|:---------|
| 兼容性 | 所有变更必须向后兼容 v2.14.0；T1-T4 架构为增量增强，不破坏现有测试流程；版本号同步为流程增强，不破坏现有发布流程 | 对照 v2.14.0 SKILL.md 检查无删除现有功能 |
| 可维护性 | 新增内容必须遵循现有 SKILL.md 格式规范（Markdown 表格、代码块、章节层级）；术语全文一致 | 格式检查 + 术语一致性检查 |
| 可追溯性 | 所有变更可追溯到 Backlog BL-ID 和设计文档 v1.4.0 | 需求追溯矩阵覆盖 100% |
| 性能 | 不适用（规范框架项目，无运行时性能指标） | N/A |

---

## 8. 数据需求

> 不适用。DevFlow 为规范框架项目，不涉及数据库模型设计。配置文件（devflow-config.json/state.json）的数据结构变更已在 F4 中定义。

---

## 9. 权限与安全需求

> 不适用。DevFlow 为规范框架项目，不涉及用户权限管理。版本号单一事实源修复（F4）涉及配置文件完整性，已通过 validate-version-header.ps1 Phase 1 一致性检查保障。

---

## 10. UI/UX 需求

> 不适用。DevFlow 为规范框架项目，无用户界面。所有变更为 SKILL.md 文档和 PowerShell 脚本。

---

## 11. 接口与集成需求

| 接口 | 描述 | 约束 |
|:-----|:-----|:-----|
| testing-stage-execution ↔ api-contract-management | T1 契约层依赖 api-contract-management 的路由映射表 diff 能力（F2）；testing-stage-execution 强制测试矩阵需交叉引用 api-contract-management | 双向引用关系需建立 |
| testing-stage-execution ↔ Stage4 产出物清单 | T1-T4 架构新增的 5 项产出物需在 Stage4 清单中注册（F3） | 产出物路径模式需一致 |
| release.ps1 ↔ devflow-config.json ↔ state.json | 发布流程自动同步链路：devflow-config.json devflowVersion → state.json（F4） | 同步方向单向，不可反向覆盖 |
| code-version-backup-management ↔ devflow-init | Git hook 安装流程纳入 devflow-init 初始化步骤（F5） | 安装步骤需幂等，支持重复执行 |

---

## 12. 约束和排除项

### 约束

- 不得扩大 Step 0 已批准的范围（5 项需求，~582 行）
- T1-T4 架构方案以 v1.4.0 为准，不得在实施中引入 v1.4.0 以外的新设计
- version.json 弃用决策已执行，不可回退
- release.ps1 修改需保留原始流程作为回退方案

### 排除项（Step 0 已确认不包含）

- V260-001（Agent 协作协议）— 需预研，待分配
- V260-003（追溯链可视化）— 需预研，待分配
- V260-004（并行阶段执行支持）— 需预研，待分配
- V260-005（国际化文档模板）— P2，待分配
- V260-039~043（全自动循环架构）— v3.0 专属
- V213-001（审计执行确定性加固）— P1，待分配（v2.14.0 已部分覆盖）

---

## 13. 需求分析发现项

### 发现项 R-001（⚠️ 低）：根因定位手段数量不一致

| 数据源 | 根因定位手段数量 |
|:-------|:---------------:|
| Backlog BL-215-001 | 7 种 |
| Phase 迭代计划 v1.5 | 7 种 |
| 设计方案 v1.4.0 | 6 种（日志分析/堆栈追踪/直连复现/DOM 检查/源码检查/错误处理检查） |

**影响**：非阻塞性。设计方案 v1.4.0 为权威源，实际包含 6 种手段。Backlog 和 Phase 计划中的"7 种"可能为计数错误。

**建议**：Step 2 设计阶段以设计方案 v1.4.0 为准（6 种手段），同步修正 Backlog 和 Phase 计划中的计数引用。

---

## 修订历史

| 版本 | 日期 | 描述 | 作者 |
|:----:|:----:|:-----|:-----|
| v1.0 | 2026-08-01 | 初始创建，5 项功能需求（F1-F5）+ 非功能/接口/约束/排除项 + 需求分析发现项 R-001 | RA-DevFlow-Dev |
