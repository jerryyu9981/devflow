# DevFlow 回滚方案 — v2.17.0

> 文档类型：回滚方案
> 版本：v2.17.0
> 日期：2026-08-18
> 作者：DO-DevFlow-Dev（DevOps工程师）

---

## 1. 回滚概览

| 项 | 内容 |
|:---|:-----|
| 目标版本 | v2.17.0 |
| 回滚目标 | v2.16.0（上一发布版本 tag）|
| 部署策略 | 直接发布（文档型项目，无蓝绿/金丝雀）|
| 回滚类型 | 代码回滚（Git）+ 文档回滚 |

## 2. 回滚路径

### 2.1 代码/文档回滚

```text
git revert <v2.17.0 commit hash>  # 推荐：保留历史
# 或
git checkout v2.16.0 -- doc/ devflow-plugin/skills/  # 恢复特定文件
# 或（仅本地）
git reset --hard v2.16.0
```

### 2.2 版本号回滚

| 配置源 | 回滚值 |
|:-------|:-------|
| devflow-config.json devflowVersion | 2.16.0 |
| project-config.json project.version | v2.16.0 |
| project-config.json lastRelease | v2.16.0/2026-08-04 |
| state.json devflowVersion | 2.16.0 |

### 2.3 IDE 技能回滚

```text
通过 update.ps1 重新推送 v2.16.0 版本技能文档
# 或手动从 archive/history/ 恢复 v2.16.0 技能文件
```

## 3. 回滚触发条件

| 触发类型 | 条件 |
|:---------|:-----|
| 自动触发 | 文档验证失败（Grep 核心标记不命中）/ 版本号不一致 |
| 手动触发 | P0 缺陷 / 规范冲突 / 使用反馈严重问题 |

## 4. 审批流程

| 场景 | 审批 |
|:-----|:-----|
| 标准回滚 | 发布负责人 + PM 双签 |
| 紧急回滚（P0）| 跳过审批，2 小时补材料，24 小时输出 RCA |

## 5. 回滚后验证

> 回滚完成后 15 分钟内必须完成验证，未通过视为回滚失败升级 P0：

| 验证项 | 命令 | 预期 |
|:-------|:-----|:-----|
| 版本号回滚确认 | grep devflowVersion devflow-config.json | 2.16.0 |
| 技能文档回滚确认 | Grep T3a 标记 | 回滚前内容 |
| tag 回滚确认 | git tag -l | v2.17.0 移除或 v2.16.0 存在 |

## 6. 数据回滚

> 本版本无数据库变更，无数据回滚需求（N/A）。

---

## 修订历史

| 版本 | 日期 | 描述 | 作者 |
|:----:|:----:|:-----|:-----|
| v1.0 | 2026-08-18 | 初始创建，定义 Git 回滚路径 + 版本号回滚 + IDE 回滚 | DO-DevFlow-Dev |
