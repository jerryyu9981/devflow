# DevFlow 单版本规划文档 — v2.14.0

> 文档类型：单版本规划文档
> 版本：v2.14.0
> 日期：2026-07-28

---

## 版本目标

| 目标 | 说明 | 成功指标 |
|:----|:-----|:---------|
| 🔒 **审计执行确定性加固** | 解决 audit-agent 能力 3 执行遗漏的根因。检查点复查抽样表中的"验证命令"列改为可直接复制粘贴执行，全流程审计报告追加"逐条执行记录"章节 | audit-agent 全流程审计报告包含"各阶段检查点逐条执行记录"，8 项检查点逐一记录执行命令和输出 |
| ✅ **变更自检规则验证** | V213-002 变更自检执行规则已写入 devflow-phase-manager + 6 个 L2 技能，但尚未在真实文件中验证其有效性。本版本运行自检规则，确认它能捕获"文件头版本号与修订历史底部不一致"问题 | 自检规则扫描 doc/ 全部文件，输出零遗漏或违规清单可追溯 |

### 包含范围

| 类型 | 项 | 描述 | 工作量 |
|:-----|:---|:------|:------:|
| **改进** | audit-agent 能力 3 检查点命令可执行化 | 检查点复查抽样表中的每条"验证命令"去除占位符，改为可直接复制粘贴到终端运行的格式；占位符统一使用 {VERSION}（与 checklist 机制一致）| 小 ~20 行 |
| **改进** | 全流程审计报告追加逐条执行记录章节 | 能力 4 报告模板新增"各阶段检查点逐条执行记录"表格，包含阶段、检查点、验证命令、实际输出、一致性判定 | 小 ~15 行 |
| **改进** | 阶段审计报告追加检查点复查章节 | 阶段审计报告（--stage N）必须包含该阶段对应的检查点复查结果 | 小 ~10 行 |
| **改进** | 更新 audit-agent.md 工作流描述 | 能力 3 工作流中明确"逐条执行并记录"的执行顺序 | 小 ~10 行 |
| **验证** | V213-002 变更自检规则有效性验证 | 遍历 doc/ 下全部 .md 文件，核对文件头版本号 vs 修订历史底部版本号一致性；对已有违规文件开具清单 | 小 ~20 行验证 |

### 不包含范围

- V260-001（Agent 协作协议）— 需预研，待分配
- V260-039~043（全自动循环架构）— v3.0 专属

## 版本风险清单

| 风险 | 级别 | 说明 | 缓解措施 |
|:-----|:----:|:------|:---------|
| 检查点命令仍含隐式依赖 | 🟢 P3 | 部分检查点依赖 Git 可用、远程可达 | 命令前标注前提条件；不可执行时标记"环境不可用"而非任意通过 |

## 版本依赖清单

| 依赖 | 类型 | 状态 |
|:-----|:----|:----:|
| v2.13.0 已发布 | 前置版本 | ✅ 就绪 |

## 发布策略草案

- 类型：修订版
- 工作量大：~55 行文档变更
- 影响范围：仅 audit-agent.md 一个文件

---

## 修订历史

| 版本 | 日期 | 描述 | 作者 |
|:----:|:----:|:-----|:-----|
| v1.0 | 2026-07-28 | 初始创建 | PM-DevFlow-Dev |
